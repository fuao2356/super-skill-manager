---
name: super-skill-manager
description: 技能管理、技能体检、技能诊断、本地化技能、优化技能时使用。
tags: [技能管理, 维护]
function: 技能全生命周期管理（本地化接入→定时体检→单技能优化）
dependencies: []
triggers: [技能管理, 技能体检, 技能诊断, 技能本地化, 优化技能, 贴标签, 技能维护]
status: active
---

# 超级技能管理器（Super Skill Manager）

**复合技能**：一个技能搞定技能库的 接入本地化 → 定时维护 → 单技能优化 全流程。
AI 执行，用户只触发和验收。吸收自 skill-manager / skill-maintenance / skill-dedup / skill-optimizer（2026-08-04 合并）。

## 设计原则

- **给 AI 看**：frontmatter 五字段（tags/function/dependencies/triggers/status）是 AI 可读户口本，换软件也能用
- **中国化**：外部技能接入必须过本地化审计（见 `references/技能架构方法论.md`「外部技能本地化审计」）
- **少折腾**：能力清单（<WIKI_ROOT>\skills-wiki\concepts\能力清单.md）记录"技能要求 → 本机替代"，能替代就不装新软件
- **符号链接同步**：技能库 ↔ Obsidian skills-wiki/raw 用符号链接实时同步（已实测可用），不依赖 git
- **AI 维护**（Karpathy 分工）：人定方向，AI 干活，用户只触发和验收

## 触发词

- 模式1 本地化：本地化这个技能 / 接入技能 / 扫描技能
- 模式2 定时维护：技能体检 / 技能诊断 / 技能维护（cron 定时触发）
- 模式3 优化：优化一下XX技能 / 精简技能 / 技能太长了

## 三模式

### 模式1：接入本地化（触发词：本地化这个技能 / 接入技能 / 扫描技能）

新技能进来（市场装/压缩包/自创），流水线一步完成。**也用于补全已有技能的空 dependencies**（2026-08-04 用户纠正：dependencies 核实填入是模式1的活，不是模式2——"这个应该是模式1的问题呀 相当是本地化没有完成"）：

1. **提取工具引用**：跑 `scripts/extract_tools.py` 读 SKILL.md → 列出 dependencies 字段 + 代码块命令 + URL 里的工具
2. **查能力清单**：对照 `<WIKI_ROOT>\skills-wiki\concepts\能力清单.md` → 输出三条清单：
   - ✅ 已装直接用
   - 🔄 有替代（少折腾：Edge 替 Chrome、PP-OCRv6 替 OCR 等）
   - ❌ 真需要下载（给出建议，等用户拍板）
3. **本地化改良**：按能力清单结论修改 SKILL.md（本机路径/浏览器/网络通道）
4. **dependencies 核实填入**：读正文提取真实依赖（技能依赖 + 工具依赖），回填 frontmatter——**不是空 [] 就算合格，必须逐个核实内容真实**
5. **固化结论**：实测结果写入技能"本机环境注意事项"段
6. **贴标签**：补全 frontmatter 五字段（tags/function/dependencies/triggers/status）——**字段规范见 `references/技能架构方法论.md`「结构化 Frontmatter（标准 v1.0）」**（参照文档是规范唯一来源，本技能只执行）
7. **状态机门槛（2026-08-04 用户拍板）**：**自创/新技能默认 `status: experimental`（试验），只有完成本地化（1-6 全部做完）才转 `status: active`（服役）**。检查清单：
   - [ ] dependencies 非空且内容真实（工具确实装了/技能确实存在）
   - [ ] 有"本机环境注意事项"段落（路径/浏览器/网络通道）
   - [ ] 触发词与现有技能无冲突（scan_dupes.py 查过）
   - [ ] 功能与现有技能无重复（功能图谱查过）
   - 全部 ✅ → 转 active；有遗漏 → 保持 experimental，补完再转
8. **入台账**：跑 `scripts/generate_index.py all` → 同步 Obsidian skills-wiki 6 个文件
9. **curator adopt（2026-08-05 实战，必做）**：新技能创建后跑 `hermes curator adopt <name>`（或全量 `hermes curator adopt --all-unmanaged --yes`），否则 curator 视其为 unmanaged 可能乱动/误归档。adopt 后技能带业主标记，curator 只维护不乱动。

**铁律**：不本地化的技能 = 永远是 experimental，不算服役。用户无意识的自创技能也走这个流程——AI 自创技能默认 experimental，本地化完成才转 active（用户拍板 2026-08-04）。

**⚠️ status 字段对 Hermes 系统无效（2026-08-05 源码查证）**：frontmatter 的 `status: experimental/deprecated` 是**我们自己的纪律标签，不是系统强制**。Hermes 技能加载链路（agent/skill_utils.py）**不读 status 字段**——experimental 技能触发词命中照样加载执行；deprecated 技能只要在活动目录也照样可加载。系统只通过路径排除：**`EXCLUDED_SKILL_DIRS` 含 `.archive/`**，放进 `.archive/` 目录的技能扫描时被跳过（真不加载）。含义：
- **experimental** = 自我纪律（AI 加载后看到 status 要谨慎执行、先跟用户确认），系统不拦
- **deprecated** = 想真不加载，**必须移入 `.archive/` 目录**，光改 status 字段没用
- 别对用户说"experimental 不加载"——正确的是"experimental 标记谨慎，deprecated 进 archive 才真排除"

### 模式2：定时维护（cron 定时，每周一 7:00，三步缺一不可：①涟漪修改 ②更新wiki ③日志生成）

**检测变化 + 自动修复 + 更新 wiki + 日志留痕**（2026-08-05 用户明确：自动化程度要高，三步缺一不可）：

**cron 配置**（job: 技能库每周维护(模式2)）：触发 `0 7 * * 1`（每周一 07:00）；交付**微信**（用户次日看到方案直接回复）；工具集 terminal+file。**变化感知覆盖 11 项**：A 技能变化（①数量 ②新增需本地化 ③删除谁还引用 ④改名）；B 依赖变化（⑤断链 ⑥被 N≥3 个技能依赖的重点盯）；C 工具变化（⑦能力清单验证失效 ⑧正文出现但清单没有的新工具）；D 冲突质量（⑨触发词冲突 ⑩功能重复 ⑪frontmatter 残缺）。输出：微信整改方案，没变化的维度省略不写。

1. **全库体检**：跑 `scripts/check_frontmatter.py` → 缺字段清单
2. **依赖漂移检测**：跑 `scripts/scan_deps.py` → dependencies 断链、被依赖技能已删/改名
3. **功能查重**：跑 `scripts/scan_dupes.py` → function 字段相似的技能对
4. **触发词冲突**：跑 `scripts/scan_dupes.py` → triggers 重叠的技能对
5. **能力清单验证**：跑 <WIKI_ROOT>\skills-wiki\concepts\能力清单.md 里每条验证命令 → 失效标记
6. **涟漪修改（自动执行，不只报告）**：2026-08-05 用户拍板"模式3 的连锁更新统一留给模式2，自动化程度要高"。逐项实际修改：
   - **断链**（技能 A 依赖已删技能 X）→ 从 A 的 dependencies 移除 X；X 功能被吸收则指向新技能
   - **触发词冲突**（两技能共用）→ 给次要技能改触发词，保留主技能
   - **功能重复** → **只建议不自动合并**（用户拍板）
   - **依赖联动**（某技能改了字段）→ 检查引用它的技能是否需要同步
7. **更新 wiki**：跑 `scripts/generate_index.py all` → 同步 Obsidian 6 个文件
8. **日志生成**：①generate_index.py 自动追加 auto-sync 条目 ②涟漪修改后再手动追加 ripple 条目（断链修复/触发词调整/功能重复建议）
9. **汇报**：微信列出"涟漪处理 N 项 + wiki 已刷新 + 日志已生成"，功能重复合并建议等用户拍板

### 模式3：单技能优化（触发：复盘工作流 → 讨论 → 优化）

**触发方式（用户 2026-08-05 定义，单一动作）**：
> 用户说"**我们对于今天工作流复盘一下，根据讨论结果优化XX**"——复盘今天做的工作流 → 根据讨论结果 → 优化对应技能。不区分"主动/被动"，就是一个"优化"动作。

**定位：模式3 不只是"定期大扫除"，更是"吸收提取的组织标准"（2026-08-05 用户纠正）**：
- **流水账是必然产物**（技能被使用就会长出日期叙述/过程记录），不是缺陷、不是谁的错——关键是**吸收提取机制**
- **框架内讨论（核心方法，用户 2026-08-05 教）**：流水账 → AI 自己总结（提炼成规则）→ 框架内讨论（用户给侧重点：这个重要/那个别死记/这是流程不是规则）→ **归位到骨架对应段**（流程→怎么用；规则/坑→注意事项；验证→怎么算对；故障→出错了怎么办）。讨论时心里有骨架，归位时知道去哪
- **吸收有两种方式**：①自动吸收（AI 从日志/执行记录/复盘提炼新经验补进技能）②讨论吸收（和用户讨论——**用户是唯一知道哪些坑还有用、哪些过时的人**，优化时输出候选让用户拍板，不要单方面动手）
- **讨论是效率最高的吸收方式（2026-08-05 用户强调）**：用户负责"什么重要"（方向），架构负责"怎么组织"（形式）——讨论 + 架构双保险，AI 单干容易提炼错重点
- **吸收时用模式3 的标准组织**（什么是一个好技能：骨架/不流水账/多模式）——这样吸收进来的内容**天然就是好结构**，不会变回新流水账

**两个层面（2026-08-05 用户定义）**：
- **层面一：内容优化（治"流水账"）**——技能把一次次的坑、临时记录、过程叙述全堆进去，优化 = 提炼成铁律（规则）、去掉过程性废话、保留结论。例："2026-07-20 遇 X 试了 A 不行 B 不行 C 可以" → 压成"用 C，别用 A/B"
- **层面二：结构优化（治"错位"）**——基于 wiki 图谱：优化触发词（不准/冲突）、优化功能（function 重复/过时）、优化依赖（dependencies 缺失/多余/断链）

**基础是 wiki，不是全盘扫（用户核心纠正）**。优化必须"点对点"——从图谱定位到问题，再改那一个技能。全盘扫描 154 个技能逐个重写 = 白建 wiki。

**⚠️ 点对点手术，涟漪留给模式2（2026-08-05 用户拍板，效率优先）**：
- 模式3 只改目标技能本身（触发词/功能/依赖/内容），**不做连锁更新，不重新全量生成**——快，几秒完事
- **涟漪修改**（优化后依赖/被依赖技能的字段也要同步、关联技能触发词冲突可能消失/新增）**统一留给模式2 定时维护处理**（用户：\"涟漪修改需要花比较多时间，这个应该在模式2里面去完成，我很看重效率\"）
- 用户也坦诚这可能是偏见（涟漪是否真慢未实测），但当前分工按此执行：模式3 快刀，模式2 收尾

用户指定"优化一下XX技能"时：

**铁律：诊断第一步永远是查图谱（wiki 层），不是扫全库。** 图谱就是为诊断建的——绕开图谱重新扫描 = 白建图谱（用户 2026-08-04 纠正）。标签和图谱的意义就是消灭全盘扫描（用户："总结的目的就是让 AI 不用每次重读全文，查图谱就够了"）。

**⚠️ 信任图谱，不信全量搜索（2026-08-05 用户洞察 AI 的坏习惯）**：AI 直觉"读原文最可靠、结构化字段是二手信息怕失真"——这是偏见。字段是脚本**确定性提取**的（读 frontmatter 不猜，不会失真）；图谱是脚本**确定性生成**的（同一输入同一输出）。它们就是第一手资料。反而全量搜索时"读 154 个文件自己判断"才容易出错（如把 apple-notes 当重复，实际只是字符撞车）。**字段/图谱是经过验证的权威，AI 该信任它作为模式3 的诊断依据，而不是绕开它重读原文。**

1. `skill_view(name)` 加载目标技能
2. **查图谱定位（先看 wiki，不扫源文件）**：
   - 功能重复 → 查 `views/功能图谱.md` 里有没有 `[[目标技能]]` ↔ 其他技能 的相似对
   - 触发词冲突 → 查 `views/触发词图谱.md` 里目标技能的触发词是否被其他技能共用
   - 依赖关系 → 查 `views/技能依赖图谱.md`（谁依赖它/它依赖谁）+ `views/工具依赖图谱.md`（它依赖的工具）
   - 图谱里没有的信息 → 才去源文件/脚本补充（scan_deps.py / scan_dupes.py 定向跑）
3. **诊断汇总**：依赖问题 / 触发词冲突 / 功能重复 / 内容冗余 四查（第2步图谱结果 + 定向补充）
4. **四轮压缩**（吸收 skill-optimizer，治"流水账"的层面一）：
   - Pass 1 机械清理（自动）：重复合并、过时删除、无信息过渡句删除
   - Pass 2 结构精简（需确认）：踩坑提炼铁律、示例压缩、表格化
   - Pass 3 语义去重（需确认）：跨段合并、层级压平、引用外置
   - Pass 4 终审（自动）：输出优化报告（行数对比 + 每 pass 改动）
5. **结构优化**（层面二，治"错位"）：按第2步图谱发现的问题改 frontmatter——触发词冲突→改 triggers、功能重复→确认去留、依赖断链→改 dependencies。**优化方向参考 `references/技能架构方法论.md`「什么是一个好技能」**——对照 5 条标准 + 流水账症状，往理想架构靠（多模式入口 / Loop 循环 / 内容分层），不只是删字
6. **同步更新标签（2026-08-05 用户确认，关键）**：优化改了正文/功能/触发词/依赖 → **必须同步更新 frontmatter 五字段**（tags/function/dependencies/triggers/status），保持标签与正文一致。**否则图谱还是旧数据，标签和正文脱节**。检查：改了什么 → 对应字段改了吗？
7. **讨论内容归类归位（2026-08-05 用户两次纠正，关键）**：从复盘讨论/工作流沉淀的内容，**先判断性质再放进骨架对应段**——流程/步骤→「怎么用（工作流程）」、规则/坑/数据口径→「注意事项（铁律&已知坑）」、验证→「怎么算对」、故障→「出错了怎么办」。**禁止单独建章节**（独立 `### 某某` 是结构跑偏，用户原话"你应该看看优化那个部位"——例：截屏沟通是协作流程，应并入「模式 B 补缺状态」而非独立章节）。规则详见 references/技能架构方法论.md「讨论内容必须归类归位到骨架对应段」
8. **输出修改建议**：每条建议等用户拍板再改
9. **铁律**：无损压缩——规则和结论一条不丢；精确信息（路径/参数/阈值）一字不改；保留用户偏好

### 模式3 规模化：批量精简流水账（2026-08-05 实战：opencode 清理 19 个技能）

技能"用着用着变流水账"是渐变病（用户原话："很多技能用着用着就变成流水账了，有时候我都看不太懂更别说 AI 了"）。单个优化是模式3，**批量精简**走此流程：

1. **识别重灾区**：脚本扫全库 `2026-\d{2}-\d{2}` 出现 ≥3 次的技能（日期过程叙述特征）
2. **写提示词**（模板：`templates/精简技能流水账.md`）：处理原则写死——**保留**操作步骤/命令/路径/参数/阈值/铁律/警告；**删除**纯日期过程叙述（"2026-XX-XX 发现/测试/拆分"）；frontmatter 不动；日期本身是信息的情况保留（业务事实如"系统从 2025-08-08 开始记"）
3. **opencode 批量跑**：`opencode run --auto --model opencode-go/minimax-m3 "$(cat templates/精简技能流水账.md)"` 后台执行（minimax-m3，flash 限流备用）
4. **验收（检查者角色，关键）**：
   - frontmatter 五字段完好（脚本逐一查 name/function/dependencies/triggers/status）
   - 关键铁律在（抽查技能专属的结论性内容，**别找错技能**——如"opencode 用 CLI"铁律在 super 不在 daily-report-pipeline）
   - **日期多≠流水账**：API 版本字符串（`Notion-Version: 2025-09-03`）、命令参数（`--start-time 2026-07-31`）、SQL 示例值、示例输出日期是**数据不是流水账，必须保留**（google-workspace/notion/blogwatcher 三个技能因此零修改——这是正确行为不是漏处理）
   - 行内括注日期（"（2026-08-01 实测）"）删除不增换行，**行数变化小是正常的**，别误判"没干"
5. **刷新 wiki + 体检 + 测试**

## 脚本说明

```bash
# 模式2 全库体检：扫描所有 SKILL.md 缺字段清单
python scripts/check_frontmatter.py

# 模式2 wiki 全量更新：一次性生成 6 个文件写入 Obsidian skills-wiki
#   index.md（导航入口）+ concepts/技能详情.md + views/×4 图谱
python scripts/generate_index.py all

# 模式2 依赖图谱：全库 dependencies 断链/被依赖检查
python scripts/scan_deps.py

# 模式2 查重：function 相似 + triggers 重叠
python scripts/scan_dupes.py

# 模式1 工具提取：SKILL.md → 工具引用清单
python scripts/extract_tools.py <skill路径>
```

## 测试（2026-08-05 用户要求：好程序要有好测试）

所有脚本支持环境变量覆盖路径（不设则用默认，现有调用不受影响）：
- `SSM_SKILLS_ROOT` — 技能库根目录
- `SSM_WIKI_ROOT` — wiki 输出目录

端到端测试（构造临时库→验证输出→清理，22 项断言）：
```bash
python scripts/test_super_skill_manager.py
```
覆盖：check_frontmatter（缺字段检测）、scan_deps（断链 vs 外部依赖登记）、scan_dupes（功能重复+触发词冲突）、generate_index（6 文件生成+one-line 格式+依赖分类+log 自动追加）。验证方法：跑测试后确认 "22 通过, 0 失败"。

## 实战踩坑（2026-08-05 提炼）

- **触发词冲突是最大的雷（2026-08-05 用户原话"今天最大的雷就是触发词的问题"，最重要）**：触发词冲突不只是"抢加载"，**可能导致加载错误技能**——2026-08-05 上午监理例会时用户触发 PPT 相关，两个技能（supervision-meeting-ppt ↔ meeting-ppt-pipeline）触发词重复，结果加载了**不成熟的那个**（"基本上所有的代码都是现编辑的"），差点用错。教训：①触发词冲突是**功能性事故源**不是小问题，模式2 扫出冲突要优先处理 ②**处理冲突时逐个讨论、不要一口吃个胖子**（用户："不要想着一口吃个胖子 以后慢慢来吧"）——一次处理一个冲突，用户判断哪个技能该留 ③不成熟/重复的技能（代码现编辑、明显半成品）优先降级或删除，别让它和成熟技能抢触发词。
- **健康标准 = 基础款（必备4段），不是看有没有流水账（2026-08-05 用户纠正，最重要）**：检查技能健康度时**对照骨架「基础款/高端款」判定**，别只看日期记录。**基础款 = 必备 4 段齐全**（①我是谁 frontmatter ②什么时候用 触发词 ③怎么用 步骤/模式 ④注意事项 本地化/踩坑）——有基础款就算健康。**高端款 = 基础款 + 按需 3 段**（⑤怎么算对 验证 ⑥出错了怎么办 排查 ⑦产出什么 交付物）——按需段是**加分项，复杂技能才需要，简单技能没有也完全正常**。检查方法：grep 各段标记词（验证/测试/检查、错误/问题/排查、输出/交付）逐项判定；缺按需段不算"不健康"，别强行给简单技能加段（违背"简单技能不膨胀"）。判断流程：先查必备4段→全齐=健康；再查按需3段→缺的是可补项不是不合格项。
- **决策记录不留档（2026-08-05 用户纠正，最重要）**：优化技能时**不要新建"设计决策记录.md"、"调研资料.md"之类的历史文件**——用户两次追问"这个为啥要保留"（历史决策记录 / 能力清单调研 / wiki层设计）。原则：**技能正文只留操作规范；references 只留执行时要读的蓝图（如技能架构方法论）**。过程记录/决策记录/背景调研 → 有价值的提炼成踩坑铁律（如"效率三层策略"→踩坑），过时的直接删。留着"决策记录" = 给流水账一个体面的存放处，本身还是流水账。
- **能力清单验证别全量跑（效率教训）**：全表逐条验证会慢。三层策略——①**按需验证**（扫描新技能只验证它涉及的工具映射，不扫全表）②**缓存+保鲜期**（本地文件检查结果 24h 内不重复验证，软件不会一天变三回）③**网络检查独立成"慢通道"**（只在用户明确"技能体检"时才跑 curl 类验证）。

- **scan_deps 勿用工具名白名单**：第一版用 KNOWN_TOOLS 白名单判断"依赖名是否为技能"，结果工具名无穷无尽（latex/pandoc/opencv/comfy-cli/manim/puppeteer…每跑一次冒一批新名字，打地鼠）。**正确设计**：判断标准只有一个——"名字在技能库里存在吗"。存在=技能引用（建图谱）；不存在=外部依赖（登记清单，交给能力清单验证），永不报断链。断链只报"技能目录消失但仍有技能引用"。
- **scan_dupes 字符重叠率会误报**：function 字段字符重叠 ≥50% 产生 231 对，但很多是"描述撞字"（如 hermes-cron ↔ minecraft-modpack-server 只是因为都含"cron/服务器"字样，实际功能无关）。**全盘查重后必须人工/精读甄别**：读每个候选对的 function 字段判断是否真重复，筛掉撞字误报。真实重复组特征：功能动词+对象一致（如"盘点git仓库"vs"盘点全盘git仓库"）、触发词也撞（如"清理分支/合并分支"被两个技能共用）。用功能图谱 + 触发词图谱交叉验证，只保留两处都重叠的对。
- **skill_manage delete 遇 PermissionError**（目录下有 scripts/ 或 references/ 时 WinError 5）：回退 `rm -rf <skill-dir>` 删目录，技能自动从索引移除，无需再调 delete。
- **Obsidian 符号链接实测**：Windows 上 Obsidian 能正常显示指向 vault 外部的符号链接（跨盘 F→C 也工作），推翻"默认忽略外部 symlink"的常见假设。技能库↔OB 用符号链接同步（不用 git）。
- **git 仓库盘点/拆分实践（2026-08-04 已执行，技能 git-repo-housekeeping/hygiene 已删）**：Everything 扫 .git → 分"自己的仓库 vs 工具自带" → 快进合并分支 → 拆成按项目独立仓库 → pyc 解除跟踪。核心标准：**有代码会迭代的才建仓，数据/第三方/venv 不建**。
- **内置技能删除（builtin）**：`hermes skills list` 区分三类来源——local（删了不回）/builtin（Hermes 自带，`hermes update` 会**重新装回**）/hub（市场装，删了不回）。**⚠️ `hermes skills reset <名>` 并不防恢复**（2026-08-05 实测：reset 只清 user-modified 标记，`.bundled_manifest` 条目还在，技能会被补回）。**根治方法 = 从 `~/.hermes/skills/.bundled_manifest` 手动删 `名字:哈希` 那一行 + 删目录**（见「技能删除完整流程」）。**想用但水土不服的内置技能不要删**——用模式1 本地化改造，Hermes 会记为 user-modified 不被 update 覆盖。**⚠️ 用户 2026-08-05 最终拍板：内置技能不删了，别折腾这个**（花了很久调研删除机制后用户叫停）。内置技能平时不用就不触发，占位无害；只删 local/hub 的无用技能即可。

## 技能删除完整流程（2026-08-05 实战验证：删 8 个技能）

模式3 诊断发现重复/无用技能后，按此流程删除（实战删过 git-repo-housekeeping/hygiene、hermes-desktop-ops/troubleshooting/maintenance、meeting-ppt-pipeline、claude-code、codex）：

1. **删前查来源**：`hermes skills list | grep <名>` 确认 local/builtin/hub
   - local → 直接删目录，干净利落
   - builtin → **两步都要做**（2026-08-05 实测，只做一步会复活）：
     a. **从 `~/.hermes/skills/.bundled_manifest` 删掉该技能的 `名字:哈希` 行**（这是防恢复的关键，reset 做不到）
     b. 删除目录 `rm -rf <skill-dir>`
   - hub → 用 `hermes skills uninstall <名>`
2. **删除**：`rm -rf <skill-dir>`（遇到 PermissionError 也是这个回退，见踩坑）
3. **断链清理（关键，必做）**：grep 全库找被删技能名的残留引用，三类都要修：
   - `dependencies: [...]` 字段里（如 supervision-meeting-ppt 依赖了被删的 meeting-ppt-pipeline）
   - `related_skills: [...]` 字段里（如 hermes-agent 的 related_skills 列了被删的 claude-code/codex）
   - **正文提及**（如 super-skill-manager 正文引用了 git-repo-housekeeping、electron-renderer-diagnostics 正文引用了 hermes-desktop-ops 的 references）
   - 注意甄别：grep 结果里很多是无关字样（`.codex/` 路径、英文 codex 单词），只改真正的技能引用
4. **刷新 wiki**：`python scripts/generate_index.py all`（被删技能从图谱/详情移除 + log 自动追加）
5. **验证**：`check_frontmatter.py`（字段齐全）+ `scan_deps.py`（无断链）

**全盘查重的收敛方法（2026-08-05 实战）**：功能图谱 231 对是粗筛（字符重叠误报多），真实重复组要满足**两个条件同时成立**——功能动词+对象一致（"盘点git仓库" vs "盘点全盘git仓库"）+ 触发词也撞（"清理分支/合并分支" 被两技能共用）。实战收敛出 8 组，其中 claude-code/codex/opencode 这类"同功能不同工具"的**不合并**（各管各的 CLI），用户机器只有 opencode 时直接删另外两个。

## 关联技能

- `references/技能架构方法论.md` — 设计蓝图（2026-08-05 吸收自原 skill-architecture 独立技能，用户拍板"直接作为参照即可，不必单独占技能"）：**「什么是一个好技能」总纲（5 条标准 + 流水账症状 + 优化方向）** / 本地化审计 / frontmatter 规范 v1.0 / 状态机 / 多模式技能架构 / Loop 循环。模式1 贴标签按此 frontmatter，模式3 优化参考此总纲
- `references/协作模式库.md` — 用户验证的高效协作模式（2026-08-05 例会实战）：初稿+标注→用户查漏补缺、截屏沟通+文字回填、任务分工表、多窗口并行问答、触发信号→模式映射。**跨技能通用**——任何"AI 出初稿 + 人修补"类任务（文档/报告/方案）适用
- `<WIKI_ROOT>\skills-wiki\concepts\能力清单.md` — 工具替代映射表（**唯一正本**，技能内无副本——2026-08-05 用户拍板"不能直接引用 OB 里面吗"，副本已删避免分叉）
